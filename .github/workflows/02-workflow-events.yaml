name: 02 - workflow events


on: 
  pull_request: 
  workflow_dispatch: 
  
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
     - name: Show the trigger
       run: echo "Ive been triigered by ${{ github.event_name }}"
